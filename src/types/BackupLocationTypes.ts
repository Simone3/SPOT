// The backup folder contract is framework-owned: SPOT only names the API it exposes on the window
export type { BackupLocation, BackupLocationApi as SpotBackupLocationApi, ChooseBackupDirectoryResult, SetBackupDirectoryResult } from 'src/framework/types/BackupTypes';
